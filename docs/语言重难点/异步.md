# 异步

异步编程是一种使程序能够在等待长时间操作（如网络请求、文件 I/O、定时器）完成时，继续执行其他任务的编程范式。

它解决了传统同步模型中的“阻塞”问题——同步代码会因等待某个操作而停滞整个线程，导致界面冻结或资源浪费。

通过将耗时操作交由底层（如浏览器或运行时）在后台处理，并利用回调、事件循环等机制在操作完成后通知主线程，异步编程极大地提升了系统的吞吐量与响应性。

在 JavaScript 等单线程环境中，异步的实现依赖于“事件循环”（Event Loop）与任务队列（Task Queue）。

开发者可以借助三种主流模式来驾驭异步逻辑：

1. 回调函数（最基本，但易陷入[“回调地狱”](../其他知识点/回调地狱.md)）
2. `Promise`（将异步操作对象化，支持链式调用与统一错误处理）
3. `async` / `await`（基于 `Promise` 的语法糖，使异步代码拥有同步风格的易读性）。

理解异步编程不仅是掌握现代 Web 开发的关键，也是编写高性能、非阻塞应用的基础。

### 4.4 Promise/A+ 关键条款（最小集）

- Promise 只有三态：`pending`、`fulfilled`、`rejected`，且状态一旦变更不可逆
- `then(onFulfilled, onRejected)` 必须返回一个新的 Promise（记为 `promise2`）
- `onFulfilled` 仅在 fulfilled 时调用，`onRejected` 仅在 rejected 时调用
- 同一个 Promise 可以多次调用 `then`；对应回调按注册顺序执行
- 回调必须异步触发（当前执行栈清空后再执行）
- 若处理器不是函数：
  - `onFulfilled` 非函数时，值应“透传”到下一个 `then`
  - `onRejected` 非函数时，拒因应继续向后抛出
- 回调抛出异常时，`promise2` 必须变为 rejected，拒因为该异常

### 4.5 Promise Resolution Procedure（`[[Resolve]]`）

当 `then` 回调返回 `x` 时，`promise2` 的状态由解析过程决定：

- 若 `promise2 === x`，必须以 `TypeError` 拒绝（防止循环引用）
- 若 `x` 是 Promise，则采用其最终状态
- 若 `x` 是对象/函数且含 `then`，按 thenable 处理（同化）
- 取 `then` 或执行 `then` 过程中若抛错，`promise2` 需 rejected
- 对 thenable 的 `resolve/reject` 调用只允许首次生效（防止多次决议）
- 若 `x` 为普通值，`promise2` 直接 fulfilled，值为 `x`

### 4.6 Promise 设计建议

- 每条异步链路保留统一错误出口
- 避免在 `then` 中混入过多业务分支
- 封装 Promise 工具函数时，优先遵循 A+ 解析语义

## 5. `async/await`

### 5.1 语法本质

- `async` 函数返回 Promise
- `await` 暂停当前 `async` 函数，不阻塞主线程

### 5.2 常见写法

- 串行：多个 `await` 按顺序等待
- 并行：先创建 Promise，再统一 `await`

### 5.3 错误处理模式

- 局部 `try/catch`
- 函数级统一捕获
- 与全局错误监控配合

## 6. 并发控制

### 6.1 `Promise.all`

- 全成功才成功
- 任一失败立即失败（fail-fast）

### 6.2 `Promise.allSettled`

- 等待全部结束
- 适合“部分失败可接受”场景

### 6.3 `Promise.race`

- 最先 settle 的结果生效
- 适合超时兜底与竞速策略

### 6.4 `Promise.any`

- 任一成功即成功
- 全失败时抛 `AggregateError`

### 6.5 并发数限制（扩展）

- 大批量请求需限流（如分批/队列）
- 防止服务压垮或浏览器资源耗尽

## 7. 取消与超时

### 7.1 取消机制

- `AbortController` / `AbortSignal`
- 与 `fetch` 等 API 配合

### 7.2 超时处理

- `Promise.race` + 超时 Promise
- 超时后记得中断或清理资源

### 7.3 资源清理

- 取消定时器
- 释放监听器
- 避免内存泄漏

## 8. 常见陷阱与调试建议

### 8.1 常见陷阱

- 忘记 `await` 导致拿到 Promise 而非结果
- `forEach` + `async` 误以为会按顺序等待
- Promise 链中漏 `return` 导致链路断裂
- 只写成功分支，不处理拒绝分支
- 并发场景下共享可变状态造成竞态条件

## 9. 练习题建议

### 9.1 基础题

- 判断多段代码输出顺序（同步/微任务/宏任务）
- 将回调风格函数改写为 Promise

### 9.2 进阶题

- 使用 `async/await` 重构 Promise 链
